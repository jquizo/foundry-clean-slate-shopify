# Files excluded from Shopify CLI operations (push, pull, dev).
# Syntax is the same as .gitignore.

# Editor atomic-write temp files — ignored to prevent upload errors
# when the CLI watcher picks them up mid-save.
**/*.tmp*

# Non-theme root files
README.md
.gitignore

# Docs and planning
docs/**

# Reference directory — not part of the deployable theme
foundry-clean-base/**

# Playwright MCP screenshots / artifacts
.playwright-mcp/**
